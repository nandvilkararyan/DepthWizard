fastapi>=0.100.0
uvicorn[standard]>=0.20.0
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.40.0
rasterio>=1.3.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scipy>=1.10.0
pillow>=9.5.0
pydantic>=2.0.0
python-multipart>=0.0.6
# Module D — Point cloud & surface mesh
trimesh>=4.0.0          # required for GLB export (Three.js overlay)
# open3d>=0.18.0        # optional: high-quality Poisson backend (Python ≤ 3.12 only)
                        # without open3d, the SciPy Delaunay fallback backend is used
